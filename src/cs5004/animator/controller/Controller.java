package cs5004.animator.controller;

/**
 * An interface representing an animation Controller. Contains the animate method.
 */
public interface Controller {

  /**
   * Runs the timer object and produces the animation.
   */
  void animate();
}